import { TrackFilterPipe } from '../objects-manager/track-filter.pipe';

describe('TrackFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
